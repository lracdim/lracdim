---
layout: layouts/base.njk
title: Work
permalink: /work/
description: "Client work, applications, portfolio demos, and redesign concepts by John Carl Dimatulac."
---
<section class="shell portfolio-section page-intro dim-head" data-dimension="work"><p class="overline"><span class="dim-head__index">01</span> / Work / Architecture</p><h1>WORK<br><span class="dim-head__descriptor">Projects and systems</span></h1><p class="dim-head__verbs" aria-label="Structure of each case study"><span>Problem</span><span class="dim-head__arrow" aria-hidden="true">→</span><span>System</span><span class="dim-head__arrow" aria-hidden="true">→</span><span>Architecture</span><span class="dim-head__arrow" aria-hidden="true">→</span><span>Result</span></p><p>Client work, internal tools, working demos, and independent concepts. Each case study states the problem, what was built, why it was designed that way, and what shipped. Labels keep the distinctions honest.</p></section><section class="shell portfolio-section work-collection"><div class="project-showcase">{% for project in portfolio %}{% if project.featured %}<article class="project-feature" data-enter><a class="project-image" href="/work/{{ project.slug }}/" aria-label="Explore {{ project.title }}"><img src="/assets/img/projects/{{ project.image }}.webp" width="{{ project.imageWidth }}" height="{{ project.imageHeight }}" alt="{{ project.title }} homepage" loading="lazy"></a><div class="project-caption"><div><p class="project-type">{{ project.kind }}</p><h2><a href="/work/{{ project.slug }}/">{{ project.title }} ↗</a></h2></div><p>{{ project.summary }}</p></div></article>{% endif %}{% endfor %}</div><h2 class="collection-heading">More of the build.</h2><div class="project-index">{% for project in portfolio %}{% if not project.featured %}<a href="/work/{{ project.slug }}/"><span class="project-type">{{ project.kind }}</span><h3>{{ project.title }}</h3><p>{{ project.summary }}</p><span aria-hidden="true">↗</span></a>{% endif %}{% endfor %}</div></section>{% include "partials/studio-contact.njk" %}
